# Metadata

A [note](./notes.md)'s metadata consists of its machine-readable attributes. At
minimum, it includes

- the notes's [identifier](./identifiers.md),
- its creation time (`ctime`), and
- its last modified time (`mtime`).
